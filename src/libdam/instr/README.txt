INSTR

These subroutine perform various actions on c-strings in-place.
That is, they operate on the strings in the same buffers the strings
are locaed in, rather than forming a result in a separate result
buffer.

