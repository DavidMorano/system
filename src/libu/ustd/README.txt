USTD

This is a collection subroutines that convert raw UNIX® system calls
(or in some or more cases a C-STD library call) into a suboutine
that returns a 'sysret_t' type rather than a 'unixret_t' or 'errno_t',
or even something else.

