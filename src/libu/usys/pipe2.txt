PIPE2

This needs to be emulated on Apple Darwin (since that OS does not have
this call).

----

                if (of & O_CLOEXEC) {
                   if ((rs = u_closeonexec(pipe[0],true)) >= 0) {
                       rs = u_closeonexec(pipe[1],true) ;
                   }

                }
                if (rs < 0) {
                    close(pipes[0]) ;
                    close(pipes[1]) ;
                }


