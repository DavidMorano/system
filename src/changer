#!/usr/extra/bin/ksh
# CHANGER

NAMES=
NAMES="${NAMES} sbuf_char "


DN=/dev/null

for F in "${@}" ; do
  if [[ -r "${F}" ]] ; then
    for N in ${NAMES} ; do
      if fgrep "${N}" $F > ${DN} ; then
      ed - $F <<\EOF
g/sbuf_char/s//sbuf_chr/g
	w
	q
EOF
      fi
    done
  fi
done


