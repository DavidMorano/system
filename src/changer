#!/usr/extra/bin/ksh
# CHANGER

NAMES=
NAMES="${NAMES} TMTIME_ZNAMESIZE "


DN=/dev/null

for F in "${@}" ; do
  if [[ -r "${F}" ]] ; then
    for N in ${NAMES} ; do
      if fgrep ${N} $F > ${DN} ; then
      ed - $F <<\EOF
g/TMTIME_ZNAMESIZE/s//TMTIME_ZNAMELEN/g
	w
	q
EOF
      fi
    done
  fi
done


