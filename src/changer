#!/usr/extra/bin/ksh
# CHANGER

NAMES=
NAMES="${NAMES} pip->f.list"

DN=/dev/null

for F in "${@}" ; do
  if [[ -r "${F}" ]] ; then
    for N in ${NAMES} ; do
      if fgrep -q "${N}" $F > ${DN} ; then
      ed - $F <<\EOF
g/pip->f.list/s//pip->pf.lister/g
	w
	q
EOF
      fi
    done
  fi
done


