#!/usr/extra/bin/ksh
# CHANGER

NAMES=
NAMES="${NAMES} utypealiase.h"


DN=/dev/null

for F in "${@}" ; do
  if [[ -r "${F}" ]] ; then
    for N in ${NAMES} ; do
      if fgrep "${N}" $F > ${DN} ; then
      ed - $F <<\EOF
g/utypealiase.h/s//utypealiases.h/g
	w
	q
EOF
      fi
    done
  fi
done


