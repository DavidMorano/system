#!/usr/extra/bin/ksh
# CHANGER

NAMES=
NAMES="${NAMES} ustat "

DN=/dev/null

for F in "${@}" ; do
  if [[ -r "${F}" ]] ; then
    for N in ${NAMES} ; do
      if fgrep -q "${N}" $F > ${DN} ; then
      ed - $F <<\EOF
g/struct ustat/s//ustat/g
	w
	q
EOF
      fi
    done
  fi
done


