#!/usr/extra/bin/ksh
# CHANGER

NAMES=
NAMES="${NAMES} fdliner_read "


DN=/dev/null

for F in "${@}" ; do
  if [[ -r "${F}" ]] ; then
    for N in ${NAMES} ; do
      if fgrep ${N} $F > ${DN} ; then
      ed - $F <<\EOF
g/fdliner_read/s//fdliner_getln/g
	w
	q
EOF
      fi
    done
  fi
done


