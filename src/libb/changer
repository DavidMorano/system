#!/usr/extra/bin/ksh
# CHANGER

NAMES=
NAMES="${NAMES} INCLUDE "


DN=/dev/null

for F in "${@}" ; do
  if [[ -r "${F}" ]] ; then
    for N in ${NAMES} ; do
      if fgrep ${N} $F > ${DN} ; then
      ed - $F <<\EOF
g/ INCLUDE /s// HEADER /g
	w
	q
EOF
      fi
    done
  fi
done


