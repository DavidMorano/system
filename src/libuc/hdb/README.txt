HDB (Hash-Data-Base)

This is a (very old) in-memory hash indexing object.  This object
does not store any data.  Rather, this object stores and magages
the indexing structures used to access the data.

