#!/usr/extra/bin/ksh
# CHANGER

NAMES=
NAMES="${NAMES} varsub_expand "

DN=/dev/null

for F in "${@}" ; do
  if [[ -r "${F}" ]] ; then
    for N in ${NAMES} ; do
      if fgrep -q "${N}" $F > ${DN} ; then
      ed - $F <<\EOF
g/varsub_expand/s//varsub_exp/g
	w
	q
EOF
      fi
    done
  fi
done


