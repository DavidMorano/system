#!/usr/extra/bin/ksh
# CHANGER

NAMES=
NAMES="${NAMES} constexpr"


DN=/dev/null

for F in "${@}" ; do
  if [[ -r "${F}" ]] ; then
    for N in ${NAMES} ; do
      if fgrep "${N}" $F > ${DN} ; then
      ed - $F <<\EOF
g/if constexpr/s//if_constexpr/g
	w
	q
EOF
      fi
    done
  fi
done


