#!/usr/extra/bin/ksh
# CHANGER

NAMES=
NAMES="${NAMES} A.D. "

DN=/dev/null

for F in "${@}" ; do
  if [[ -r "${F}" ]] ; then
    for N in ${NAMES} ; do
      if fgrep -q "${N}" $F > ${DN} ; then
      ed - $F <<\EOF
g/A.D./s//A-D-/g
	w
	q
EOF
      fi
    done
  fi
done


