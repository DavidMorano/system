#!/usr/extra/bin/ksh
# CHANGER

NAMES=
NAMES="${NAMES} bufstr_buf "


DN=/dev/null

for F in "${@}" ; do
  if [[ -r "${F}" ]] ; then
    for N in ${NAMES} ; do
      if fgrep "${N}" $F > ${DN} ; then
      ed - $F <<\EOF
g/bufstr_buf/s//bufstr_strw/g
	w
	q
EOF
      fi
    done
  fi
done


