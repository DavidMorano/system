#!/usr/extra/bin/ksh
# CHANGER

NAMES=
NAMES="${NAMES} vecobj_vcmp "


DN=/dev/null

for F in "${@}" ; do
  if [[ -r "${F}" ]] ; then
    for N in ${NAMES} ; do
      if fgrep ${N} $F > ${DN} ; then
      ed - $F <<\EOF
g/vecobj_vcmp/s//vecobj_vcf/g
	w
	q
EOF
      fi
    done
  fi
done


