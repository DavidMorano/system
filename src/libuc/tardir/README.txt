TARDIR (Target Directory)

This object manages a litle of "target" directories.  Duplicates
are ignored (so all directories in the resulting list are unique).
Also, non-directories are flagged as error and causes premature
exit.

