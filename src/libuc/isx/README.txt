ISX

Thse subtouines (all quite simple) determine if some condition is
met with the given input arguments.  This is just a group of
facilities that share the idea that they return a boolean on some
condition.

